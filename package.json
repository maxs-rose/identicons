{
	"name": "identicons",
	"version": "1.0.0",
	"description": "Ident Icons",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "tsup src/index.ts --format esm --watch --clean",
		"build": "tsup src/index.ts --format esm --clean --sourcemap --minify --metafile"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"prettier": "^2.7.1",
		"tsup": "^6.2.3",
		"typescript": "^4.8.4"
	}
}
